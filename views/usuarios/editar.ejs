<!DOCTYPE html>
<html>
<head>
  <title>Editar Usuario</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
  <h1>Editar Usuario</h1>
  <form method="post" action="/usuarios/editar/<%= usuario.usu_id %>">
    <input type="text" name="usu_nombre" value="<%= usuario.usu_nombre %>" class="form-control mb-2">
    <input type="text" name="usu_paterno" value="<%= usuario.usu_paterno %>" class="form-control mb-2">
    <input type="text" name="usu_materno" value="<%= usuario.usu_materno %>" class="form-control mb-2">
    <input type="email" name="usu_correo" value="<%= usuario.usu_correo %>" class="form-control mb-2">
    <select name="usu_activo" class="form-control mb-3">
      <option value="1" <%= usuario.usu_activo ? 'selected' : '' %>>Activo</option>
      <option value="0" <%= !usuario.usu_activo ? 'selected' : '' %>>Inactivo</option>
    </select>
    <button class="btn btn-warning">Actualizar</button>
  </form>
</body>
</html>