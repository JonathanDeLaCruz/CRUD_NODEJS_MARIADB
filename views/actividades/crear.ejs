<!DOCTYPE html>
<html>
<head>
  <title>Crear Tarea</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
  <h1>Nueva Actividad</h1>
  <form method="POST" action="/actividades/crear" enctype="multipart/form-data">
    <input name="act_titulo" class="form-control mb-2" placeholder="Título" required>
    <textarea name="act_descripcion" class="form-control mb-2" placeholder="Descripción"></textarea>
    <input type="date" name="act_realizo" class="form-control mb-2" required>

    <label class="form-label">Tarea asociada:</label>
    <select name="act_fktarea" class="form-control mb-2" required>
      <% tareas.forEach(t => { %>
        <option value="<%= t.tar_id %>"><%= t.tar_titulo %></option>
      <% }) %>
    </select>

    <label class="form-label">Evidencia (imagen):</label>
    <input type="file" name="act_evidencia" accept="image/*" class="form-control mb-3">
    
    <button class="btn btn-success">Guardar</button>
  </form>
</body>
</html>