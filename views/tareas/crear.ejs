<!DOCTYPE html>
<html>
<head>
  <title>Crear Tarea</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
  <h1>Nueva Tarea</h1>
  <form method="post" action="/tareas/crear">
    <input type="text" name="tar_titulo" placeholder="Título" class="form-control mb-2" required>
    <textarea name="tar_descripcion" placeholder="Descripción" class="form-control mb-2"></textarea>
    <select name="tar_completado" class="form-control mb-2">
      <option value="0">Pendiente</option>
      <option value="1">Completado</option>
    </select>
    <input type="date" name="tar_limite" class="form-control mb-2">
    <select name="tar_fkusuario" class="form-control mb-3" required>
      <% usuarios.forEach(u => { %>
        <option value="<%= u.usu_id %>"><%= u.usu_nombre %> <%= u.usu_paterno %></option>
      <% }) %>
    </select>
    <button class="btn btn-success">Guardar</button>
  </form>
</body>
</html>